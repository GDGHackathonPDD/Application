/** Clerk Frontend API URL — same as `CLERK_JWT_ISSUER_DOMAIN` in `.env.local`. */
export default {
  providers: [
    {
      domain: "https://eager-grub-72.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};
